#include <bits/stdc++.h>

using namespace std; 
#define REP(i, n) for (int i = 0; i < (n); i++)
#define MOD 1e9+7

typedef long long ll;

int main(int argc, char const *argv[])
{
    int n; cin >> n;
    return 0;
}
