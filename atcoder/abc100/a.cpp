#include <bits/stdc++.h>

using namespace std;
#define FOR(i, a, b) for (int i = (a); i < (b); i++) 
#define REP(i, n) for (int i = 0; i < (n); i++)

int main(int argc, char const *argv[])
{
    int a,b;
    cin >> a >> b;
    if (a<=8&&b<=8) cout << "Yay!\n";
    else cout << ":(\n";
    return 0;
}
