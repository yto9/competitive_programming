#include <bits/stdc++.h>

using namespace std; 
#define REP(i, n) for (int i = 0; i < (n); i++)

int main(int argc, char const *argv[])
{
    int T; cin >> T;
    REP(test, T) {
        int N; cin >> N;
        REP(i, N) {
            int t;
            int Y, Z;
            cin >> t >> Y >> Z;
            cout << 0 << endl;
        }
    }
    return 0;
}
